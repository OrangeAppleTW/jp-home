{
  "name": "jp-home",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "astro": "^6.0.2"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4.2.1"
  }
}
